@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <ul>
        @foreach(var product in Model)
        {
            <li>@product.naam - $@product.prijs - @product.omschrijving</li>
            //@tabelnaam.kollomnaam
        }
    </ul>
</div>
